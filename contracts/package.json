{
	"name": "@safenet/contracts",
	"private": true,
	"scripts": {
		"build": "forge build --force",
		"check": "forge fmt --check && forge lint --deny notes",
		"deploy": "forge script DeployScript",
		"fix": "forge fmt",
		"test": "forge test -vvv",
		"coverage": "mkdir -p coverage && forge coverage --report lcov --report-file coverage/lcov.info"
	}
}
